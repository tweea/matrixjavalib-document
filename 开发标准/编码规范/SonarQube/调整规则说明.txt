Java
	Comment the default access modifier(pmd:CommentDefaultAccessModifier)
		禁用，与格式化配置不匹配
	Custom Import Order(checkstyle:com.puppycrawl.tools.checkstyle.checks.imports.CustomImportOrderCheck)
		禁用，与格式化配置不匹配
	Empty Line Separator(checkstyle:Empty_Line_Separator)(checkstyle:com.puppycrawl.tools.checkstyle.checks.whitespace.EmptyLineSeparatorCheck)
		去除 PACKAGE_DEF，与格式化配置不匹配
	Performance - Method appears to call the same method on the same object redundantly(fb-contrib:PRMC_POSSIBLY_REDUNDANT_METHOD_CALLS)
		禁用，判断条件不足
	Use ConcurrentHashMap(pmd:UseConcurrentHashMap)
		禁用，判断条件不足
